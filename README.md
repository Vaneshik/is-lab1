# ✨ Anime Waifu Database 💖 [AI GENERATED]

[![CI Status](https://github.com/Vaneshik/is-lab1/workflows/🚀%20CI%20-%20Проверка%20что%20всё%20собирается/badge.svg)](https://github.com/Vaneshik/is-lab1/actions)

> *когда препод сказал "сделайте инфосистему" а ты сделал anime waifu database (◕‿◕)♡*

## Что это вообще такое

Kawaii система управления аниме персонажами с красивым UI! 🌸

**Стек:**
- Backend: Java EE + WildFly (олдскул но работает)
- Frontend: React с anime-стилем (✨ КРАСОТА ✨)
- База: PostgreSQL (классика)
- Деплой: Helios + VPS (работает стабильно)
- Дизайн: Glassmorphism + градиенты + анимации 💖

## Быстрый старт

### Если ты препод
```bash
curl http://213.21.252.60
# Оно работает, можете ставить зачет
```

### Если ты я из будущего
```bash
# 1. Настрой .env
cp ENV.example .env
vim .env  # вставь свои креды

# 2. Задеплой всё
./deploy-all.sh

# 3. Profit 💰
```

## Архитектура

```
┌─────────────┐     SSH     ┌──────────────┐
│   Helios    │────tunnel───│   VPS        │
│   :31337    │             │   :80        │
│  (Backend)  │             │  (Frontend)  │
└─────────────┘             └──────────────┘
       │                            │
       │                            │
       └────────PostgreSQL───────────┘
```

*Схема нарисована ASCII-артом потому что можем*

## Фичи

**Backend:**
- ✅ CRUD для Person (добавить, удалить, изменить, найти)
- ✅ Пагинация (потому что 1000 записей - это перебор)
- ✅ Фильтрация (по любому полю, даже по цвету глаз)
- ✅ WebSocket (real-time обновления)
- ✅ GitHub Actions CI (проверяет что всё собирается)

**Frontend (✨ НОВОЕ ✨):**
- 💖 Anime/Kawaii дизайн с glassmorphism
- 🌸 Красивые градиенты и анимации
- ✨ Плавающие сердечки и звездочки
- 📱 Адаптивный дизайн для мобильных
- 🎨 Светящиеся эффекты и transitions
- 🌙 Темная тема с розовыми акцентами

## API

**Base URL:** `http://213.21.252.60/person-management-system/api`

```bash
# Получить всех людей
GET /persons

# Добавить человека
POST /persons
{
  "name": "Вася",
  "coordinates": {"x": 123, "y": 456},
  "height": 180,
  "eyeColor": "BLUE",
  "hairColor": "BLACK",
  "nationality": "RUSSIA",
  "location": {"x": 1, "y": 2, "z": 3}
}

# Удалить человека
DELETE /persons/1

# Остальное в API.md (если интересно)
```

## CI/CD

Каждый пуш в `main` автоматически проверяется через GitHub Actions:

- 🔨 Backend компилируется и собирается WAR
- 🎨 Frontend компилируется React приложение
- 🐳 Docker образ проверяется на сборку

Бейдж вверху показывает статус последней сборки. Если красный - значит что-то сломалось (но это не мы).

## Деплой

**Backend на Helios:**
```bash
./deploy-helios.sh
# Ждем 40 секунд пока WildFly проснется
```

**Frontend на VPS:**
```bash
./deploy-frontend-docker.sh
# Docker идет бррррр
```

**Всё сразу (для ленивых):**
```bash
./deploy-all.sh
# Идем пить кофе ☕
```

**SSH Туннель (если оно отвалилось):**
```bash
./setup-ssh-tunnel.sh
```

## Проблемы?

**Backend не запускается:**
```bash
ssh s409858@se.ifmo.ru -p 2222
tail -f ~/wildfly.log
# Читаем стектрейс, плачем, гуглим
```

**Frontend показывает 502:**
```bash
ssh root@213.21.252.60
docker logs person-management-frontend
# Проверяем SSH туннель
./setup-ssh-tunnel.sh
```

**"У меня не работает":**
- Проверь .env файл
- Проверь что Helios не упал (он падает)
- Проверь SSH туннель
- Перезапусти всё (IT crowd way)
- Напиши мне (пожалуйста не надо)

## Структура проекта

```
is-lab1/
├── src/main/java/se/ifmo/ru/person/  # Java код
├── frontend/                           # React приложение
├── deploy-*.sh                        # Скрипты деплоя
├── database-functions.sql             # SQL для функций
└── README.md                          # Ты здесь
```

## Зависимости

- Java 17+ (современный и без легаси)
- Maven (для сборки)
- Node.js (для фронта)
- Docker (опционально)
- Доступ к Helios (обязательно)
- Кофе (критично)

## Лицензия

MIT или что там у ИТМО принято.

## Автор

Сделано с ~~любовью~~ дедлайном для ИС Лаба 1.

*P.S. Если это работает - не трогай*

---

**Статус:** 🟢 Working (на момент последнего коммита)

**Последнее обновление:** Когда успел задеплоить

**Пинг препода:** @instructor плз поставь зачет 🙏
