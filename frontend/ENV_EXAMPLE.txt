# Создайте файл .env в директории frontend с этим содержимым

# Backend Server Configuration
# Backend деплоится на удаленный сервер по SSH
# Frontend запускается локально и подключается к удаленному backend

# Адрес сервера (замените на ваш фактический адрес)
REACT_APP_BACKEND_HOST=se.ifmo.ru
REACT_APP_BACKEND_PORT=8080
REACT_APP_BACKEND_PATH=/person-management-system/api

# Для локального тестирования раскомментируйте:
# REACT_APP_BACKEND_HOST=localhost
# REACT_APP_BACKEND_PORT=8080
# REACT_APP_BACKEND_PATH=/person-management-system/api

